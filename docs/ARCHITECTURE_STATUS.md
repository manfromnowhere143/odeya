# Architecture Working Checkpoint Status

Status timestamp: 2026-08-03, Asia/Jerusalem. This is an evidence-backed working checkpoint, not a frozen Gate A candidate, operator acceptance, or implementation authorization. Product/runtime work remains prohibited.

## Locked operator constraints

- The name is **Odeya**.
- Odeya is an independent private engine and company, not an Aweb or Maestro feature.
- Sentinel, Telos, and Inbar are proof missions and requirements sources, not repositories to merge.
- The working web address is `odeya.danielwahnich.dev`; domain acquisition is outside the engine critical path.
- The engine architecture precedes runtime, deployment, integrations, and production UI implementation.
- Daniel owns visual/product design. Engine contracts constrain truthful state, authority, disclosure, and accessibility without replacing that direction.
- Open research can leave the private engine only through explicit evidence, rights, safety, authority, and externally settled publication contracts.

## Candidate architecture verdict

The leading design is a governed dual-loop research system:

1. a deterministic scientific kernel owns canonical state, contract admission, protocol exposure, authority, resource commitments, evidence custody, verification requirements, claim eligibility, recovery legality, and release settlement;
2. replaceable epistemic/model components propose hypotheses, plans, experiments, code, analyses, critiques, and graph deltas through typed, exposure-bounded contracts;
3. separately assigned verification and rule-bound adjudication stand between generated work and an eligible claim; and
4. private, thesis, and public surfaces are rebuildable non-authoritative projections. No model, cache, receipt, seal, provider response, or UI can publish by implication.

This design fits the strongest retained lessons from the three proof missions. It has not demonstrated autonomous science, scientific superiority, production readiness, or operator acceptance.

## Current machine evidence

| Area | Retained working evidence | Bounded interpretation |
| --- | --- | --- |
| Foundation corpus | 144 Draft 2020-12 schemas; 908 shared-manifest cases (244 valid and 664 known-bad); 7 founding cross-field groups; 17 declared isolated contract suites; 19 declared bounded architecture-evidence checks; 1 repository release-surface check. The exact frozen predecessor cohort is 120 schemas; the 12 side-by-side PRQ-002B and 12 side-by-side PRQ-002E resources are candidate-only | The schema corpus and affected isolated suites pass. Full-validator, exact-commit rehearsal, publication, and remote-check status must be resolved from the exact Git subject and retained release evidence rather than inferred from this row. Structural and bounded semantic evidence is not runtime or Gate A evidence |
| Guard evidence | Lifecycle has dedicated statement coverage at 222/229 and top-level condition coverage at 108/111. The retained generalized v0.2 audit separately proves 501/1260 refusal statements across 17 declared isolated checker subjects, with 759 explicitly unproved and zero crash-only detections; the assurance checker accounts for 206/287 proved guards, `prq-002-identity-cohort` for 0/127, `product-identity-profile-candidate` for 0/97 with all 97 open, and `product-identity-profile-0.3-candidate` for 24/72 with 48 open. Proof requires a syntax-valid isolated mutation, an exact suite-reported refusal, a passing restored control, and an identical repeated framed SHA-256 fingerprint; crash-like outcomes, failed controls, and unstable repeats remain unproved. The predecessor-profile suite runs serially after the fixed four-worker phase. The unpublished 502/1260 attempt and former 16-subject 477/1184 measurement are superseded; the former 431/820 result is retracted because its assurance-checker mutations tripped an unrefreshed outer byte binding rather than the intended guard | Statement reachability and top-level condition removal only. The generalized denominator excludes central architecture/release gates such as PRQ-009, which carry their own pinned known-bads. Coverage does not prove a guard enforces the right rule; the audit also provides neither exact case-ID attribution, hostile-writer/process isolation, nor independent reproduction |
| Pre-implementation surface lock | A default-deny policy admits exact top-level roots, file types, executable-capable code paths, and executable-bit paths. Six safe controls pass and 16 exact-error known-bads reject undeclared roots/code or content-addressed blobs, suffix or binary disguises, symlinks, modes, gitlinks, and staged/worktree byte divergence | Bounded repository-shape evidence only. It does not semantically classify every allowed text file, cannot inspect a Git index in a Git-less export, and can still be weakened together with its policy/cases unless exact-byte review catches the change |
| Repository release surface | One exact README identity sentence; a case-insensitive repository-wide tracked-Markdown census admitting exactly seven diagrams across four governed documents and zero elsewhere; every admitted Mermaid source rendered through the exact-byte-bound CLI/browser loop; thirty-one in-memory inventory known-bads plus four renderer-contract mutations proving alternate fences, container/nested escape, lowercase-only discovery, uppercase-extension discovery, unsafe configuration and resource-loading syntax, count weakening, early exit, signature-check removal, and CRLF byte substitution refuse; architecture-only least-privilege workflows with full-SHA Action uses; exact-version Python/Node/Java inputs; hash-locked Python dependencies; integrity-locked npm dependencies; digest-verified fallback tool archives; per-rehearsal mutable tool isolation with the formal run bound to its verified TLA+ jar; Markdown, shell, workflow, bounded dependency-freshness, and fresh-clone harnesses | The public architecture repository exists under ADR 0047 and exact-commit publication is evidence-gated. Historical checkpoint `56e8062` had three workflow families and nine jobs green. Completed first-activation checkpoint `f1f25fd` has separate four-workflow/ten-job release and post-main censuses, remote-main comparison receipt SHA-256 `442ab446a1fad1a62ec6049b978b2af53fa1b67ccbd6acebf34619ff540ea625`, and final activation receipt SHA-256 `2f9564a23bf3bde851244224eb5f69b5cfe881e39eec46b1387d5a34b85a1ab2`. At the 2026-07-19 pre-hardening observation, secret scanning plus push protection were enabled, no ruleset existed, `main` was unprotected, pull requests and all three PR merge methods were enabled, Actions admitted unpinned third-party actions, and Dependabot security updates were disabled. [ADR 0091](decisions/0091-exact-sha-two-ref-publication-sequence.md) accepts a single-direct-child, two-ref, four-workflow/ten-job sequence with separate no-bypass `main` and permanent candidate-ref rulesets; the latter allows one creation, then restricts every update and blocks deletion/non-fast-forward changes. Both rulesets require server linear history. Pull requests are disabled after every pre-existing open PR is closed; merge commits and squash merges are disabled; rebase merge remains enabled only as GitHub's inert linear-history prerequisite; and the verifier refuses `has_pull_requests` drift. REST proves repository identity and PR disablement; one fixed, identity-bound GraphQL query transported by POST proves the merge fields that REST hides after disablement, and every mutation or alternate query is refused. The push-only verifier additionally carries the exact direct-child proof. The completed first activation retains an immutable eleven-operation mutation journal and a final read-only receipt binding every candidate/main check census, promotion governance, remote comparison, protected ref, and effective rule. Active PRQ candidate bytes are present in the checked-out tree; their exact commit, rehearsal, publication, and remote-check status must be resolved from Git and their own external subject-bound receipts. The local hook is not server protection. None of this grants runtime, deployment, scientific-publication, or Gate A authority |
| Command/event surface | `CommandEnvelope` 0.5.0 and `CommandReceipt` 0.4.0 are explicitly nonconstructible structural candidates over the 121-selector design vocabulary retained from exact historical `CommandEnvelope` 0.4.0 bytes; `ResearchEvent` 0.18.0 has 135 closed branches; 66 schema-valid event fixtures and 16 schema-valid replay traces are retained within 207 direct event/trace manifest cases | Every design command/event remains non-admitted until exact immutable members, identities, and activation exist |
| Registry/root graph | Six registry/root/activation schema subjects validate; the proposed logical manifest v2 covers 9 layers, 30 modules, 47 aggregate owners, 144 schema owners, 121 command owners, and 135 event owners | Ownership and acyclic subject shape are present; no real registry snapshot, root, checkpoint, activation, or handler-set equality exists |
| Cognitive controls | 150 cases pass, including 18 bounded semantic checks over state views, graph deltas/transitions, WorkIntent/blocked WorkContract boundaries, model configuration, and routing plus retained local-dispatch refusals | Models remain proposal/evidence producers; routing is not dispatch or independence authority |
| Verification | Eight terminal package fixtures plus assignment/completion/dispute event fixtures and 49 scoped shared-manifest cases. The physical package now resolves one exact typed `PhysicalEvidenceVector`, derives IV4/physical/safety applicability, and rejects the former “No physical-world assertion” false positive | Profile-relative IV0–IV4 structure and one bounded synthetic resolver join are present; general resolution, semantic equality, reducer replay, registry admission, real physical evidence, and independent review remain open |
| Projection | Four schemas, eight fixtures, and 63 cases pass: 37 structural rejects and 26 bounded semantic cases while covering all 25 named adversarial vectors | Snapshot/redaction/equivalence/impact shapes are closed locally; the synthetic current ResearchEvent references are pinned to 0.18.0 without making a legacy-migration claim. Two independent reducers, real fanout, leakage/timing, accessibility, and exact release-set equality remain open |
| Physical science | Eight physical schemas plus the generic verification schema form a 9-schema checked DAG; 95 structural expectations and 42 bounded semantic expectations pass | Inbar unknown hardware is blocked and its experiment refused. The newly coherent verification fixture is explicitly synthetic architecture evidence, not a real physical result. Unit/frame algebra, covariance/propagation, real metrology/VVUQ/safety evidence, reducers, and accountable review remain open |
| Mathematical science | `EstimandContract` and `ScientificResultEnvelope` 0.1.0 plus optional protocol/metric bindings; 57 cases pass: 37 structurally valid, 20 structurally invalid, 37 bounded semantic checks | Sentinel’s interval crossing zero remains `inconclusive`; it cannot become support, effect absence, equivalence, or an affirmative null without the required prospective contract |
| Canonical identity | Fresh Python 3.14.2- and Node.js 20.18.3-labelled results have the same 64 vectors and 4 metamorphic relations and conform to the frozen oracle after rebinding the frozen-predecessor audit executable; the result documents changed only by their new source-lock digest. A separate adversarial meta-evaluator reproduces the comparator's acceptance of copied Python output carrying Node metadata. The predecessor profile binding passes 1 safe/26 known-bads; the WorkIntent core binding passes 1/28; the side-by-side WorkIntent 0.2 / WorkLease 0.2 / WorkContract 0.3 successor cohort passes 1/37; the exact-reference WorkIntent 0.3 cohort passes 1/40; the frozen predecessor audit covers 120 schemas and 216 fixtures. The C5 correction reissues ResearchEvent 0.18.0 and blocked WorkContract 0.19.0, then binds that exact base candidate in the separately identified WorkContract wrapper 0.20.0 | Oracle conformity is `pass` and case-projection comparison is `exact`; causal execution origin remains `unwitnessed_retained_artifact`, so source separation is `not_proven` and profile issuance remains blocked. Candidate parser, RFC 8785, envelope, digest framing, future reference shape, 21 predecessor domains, and 8 version axes are frozen for review. WorkIntent 0.3 binds exact raw source/planning candidates and the exact output schema while keeping target canonical digests null and forbidding raw-hash relabeling. Every predecessor migration class measures zero, but the predecessor profile remains unissued pending accountable review and the operator's exact-byte decision. The 0.18/0.19/0.20 reissue chain preserves predecessor identities and keeps canonical digest, membership, admission, and authority absent |
| PRQ-002A structural identity probe | [ADR 0099](decisions/0099-freeze-prq-002a-structural-identity-probe-layer.md) retains nine architecture-only schemas outside the 120-product-schema census, 21 non-issuable probe objects, 20 structured digests, and 47 isolated cases: 1 safe and 46 known-bad. Two source- and language-separated retained results carry provenance-attested current recomputation | This is bounded structural probe evidence, not independent implementation or review. No probe schema, member, snapshot, or profile is issued; no PRQ-002 closure, `EngineContractRoot`, activation, runtime, or Gate A authority exists. Product work requires new member schemas and domains plus a reviewed profile successor/reissue and explicit migration; probe identifiers must never be promoted |
| PRQ-002B product-identity successor | [ADR 0100](decisions/0100-introduce-an-unissued-scoped-product-identity-profile-successor.md) retains four standalone product-member schemas, one ordered-map commitment schema, four pure-registry successor schemas, and three schemas plus exact records for a scoped `odeya-jcs-0.2` core/evidence/migration chain. The nine product-identity schemas use nine distinct domain contracts; exact current bytes and the frozen 120-schema predecessor set reconcile to a disjoint 132-schema candidate union. The isolated suite passes 1 safe control and 19 attributed single-fault known-bads; the Gate cross-check rejects 21 mutations—16 exact-inventory or JSON-type attacks plus 5 schema-valid semantic downgrades | This is bounded, schema-valid, exact-byte architecture candidate evidence. Exact JSON type is part of the retained contract: integral floats do not satisfy integer byte/count fields, and integers do not satisfy Boolean authority/nonclaim fields. The retained structural vectors are explicit nonidentity fixtures. No product member, commitment, snapshot, canonical digest, profile member, root, or activation exists. The later PRQ-002F–I evidence is separate and does not promote this tranche. Both profiles remain unissued; accountable review and operator acceptance are absent; PRQ-002 remains open; Gate A remains blocked; and runtime is unauthorized |
| PRQ-002C raw-number prerequisite | [ADR 0101](decisions/0101-require-raw-number-token-provenance-before-profile-conformance.md) retains an architecture-only raw-token contract and 61 opaque, answer-free synthetic integer-position frames: 9 accepted and 52 refused. Source- and language-separated Python and Node implementations agree on the complete ordered staged projection; 44 suite-gate known-bads fire | This is bounded agreement for fixed integer-type and integer-valued-const microframes only. It does not itself establish generic schema-path evaluation, number-position semantics, nine-domain framing, ordered-map laws, cross-object replay, offline resolution, organizational independence, independent-host reproduction, or full successor-profile conformance. The later PRQ-002F–I evidence is separately scoped to the 0.3 candidate and does not amend the frozen, unissued `odeya-jcs-0.2` bytes. No product identity, PRQ-002 closure, Gate A acceptance, runtime authority, or publication authority exists |
| PRQ-002D non-product prehash replay | [ADR 0102](decisions/0102-prove-non-product-prehash-schema-registry-replay.md) retains one exact-predecessor contract, 68 opaque virtual-file frames, one fixed private oracle, complete Python and Node results, two self-attested execution receipts, and one exact comparison receipt. One frame is accepted and 67 are refused under the frozen order; 77 named parent-gate mutations each return their declared singleton guard | This is bounded differential evidence for a synthetic two-member, prehash schema-registry replay before any structured or product identity. The retained source controls are exact inventories, not process sandboxes or general static analyses, and the receipts are not independently witnessed process evidence. It does not itself establish canonicalization-profile conformance, offline resolution, dependency-closed product registries, cross-object product replay, organizational independence, product identity, admission, PRQ-002 closure, Gate A acceptance, runtime authority, or publication authority. The later PRQ-002E–I chain does not change this replay's bounded interpretation |
| PRQ-002E side-by-side profile construction | [ADR 0103](decisions/0103-construct-side-by-side-odeya-jcs-0-3-candidate.md) retains twelve final-only `odeya-jcs-0.3` schema resources, nine schema-valid structural-nonidentity fixtures, three ordered candidate records, and a separate seven-output observation transaction over the resulting fifteen immutable subjects. Source- and language-separated Python 3.14.2 and Node.js 24.18.0 observers agree on the complete exact-byte fifteen-row projection; the product-authoring transaction is reproducible as exactly 24 outputs and preserves the frozen `0.2` cohort | This is construction and byte-agreement evidence only. The later PRQ-002F–I tranches add bounded trace, serialization, domain-frame, and declared-universe resolution evidence without changing these bytes. Organizational independence, independent-host reproduction, accountable review, operator acceptance, product identity, profile/resource issuance or admission, PRQ-002 closure, Gate A acceptance, runtime authority, and publication authority remain absent |
| PRQ-002F numeric-trace conformance | [ADR 0106](decisions/0106-prove-raw-aware-numeric-trace-conformance-for-the-0-3-cohort.md) retains two source- and language-separated complete raw-token applicability traces and cross-object conformance projections over the exact fifteen PRQ-002E subjects, with exact-byte comparison and third-path known-bad validation | This closes the declared per-subject numeric-trace unit only. It does not canonicalize a product, issue a profile, establish organizational independence, or extend conformance beyond the fifteen frozen subjects |
| PRQ-002G JCS serialization conformance | [ADR 0107](decisions/0107-prove-profile-bounded-jcs-serialization-conformance.md) retains two zero-dependency implementations of the exact RFC 8785 interpretation and refusal boundary pinned by `odeya-jcs-0.3`, plus an answer-free corpus, exact comparison, and third-path known-bads | This is serialization evidence only for the profile's admitted integer-token set. It computes no cohort identity or digest and does not issue or admit the profile |
| PRQ-002H product-domain governance | [ADR 0108](decisions/0108-prove-nine-product-domain-frame-governance.md) retains two source- and language-separated governance paths over the nine frozen structural-nonidentity fixtures, including complete token traces and per-domain refusal evidence | Each product-domain schema demonstrably governs one frozen nonidentity fixture within the declared suite. No fixture becomes a product identity, admitted member, runtime object, or scientific result |
| PRQ-002I offline resolution | [ADR 0109](decisions/0109-prove-dependency-closed-offline-resolution.md) retains two source-separated projections and a third-path validator over one declared 199-member universe: 6,456 schema-reference edges resolve offline and 241 declared digest bindings replay from repository bytes; 26 embedded known-bads retain the declared refusal boundary | Dependency closure is proved only for the declared universe and three declared binding shapes. Four historical residue identities remain explicitly unresolved. No product identity, issuance, admission, PRQ-002 closure, Gate A acceptance, or runtime authority follows |
| Bounded formal models | 7 distinct safe TLA+ models, 8 safe executions, and 30 intended counterexamples pass in one integrated sequential harness; the alternate cognitive execution uses a second seed/fingerprint profile | This is bounded model evidence on one model/checker/host lineage. It is not an independent proof or runtime verification |
| First-slice admission | Candidate 0.2 retains exact representational sets—43 required / 78 outside commands, 60 event types, 25 aggregate/state/reducer families, 11 owners, and one P0 prerequisite—while keeping C5/PRQ-009 blocking; 12 safe references and 61 known-bads pass the bounded adversarial checker | [ADR 0090](decisions/0090-preserve-the-resource-claim-when-work-lease-ends.md) corrects `C5-WORK-LEASE-RELEASE-CLAIM-001` in unissued ResearchEvent 0.18.0: WorkLease release retains the exact claimed reservation for separate ResourceLedger settlement. The evidence dereferences a partial start cohort, a complete three-event report cohort, and later settlement without digest recomputation or reducer replay. A constructible resolved WorkIntent, exact admitted assignment binding, members, root, replay, review, and activation remain absent. Nothing is admitted or executable |
| PRQ-009 assignment order | One finite contract audits 14 active consumers and 2 explicitly superseded artifacts; 14 pinned known-bads freeze `WorkIntent → atomic 13-event verification.assign → derived WorkContract → separate attempt.start`, reject legacy cycles/extra authority, and pin the exact 54-fragment consumer matrix | The contradiction is corrected, but PRQ-009 remains `unresolved_blocking`: required profile/member/reducer/root identities, replay/refinement, independent review, and Gate A acceptance are absent |
| PRQ-013 human-decision assurance | ADRs [0092](decisions/0092-bind-human-decisions-through-an-external-assurance-wrapper.md), [0093](decisions/0093-co-bind-the-confirmation-gesture-through-a-two-phase-challenge.md), [0095](decisions/0095-reissue-human-decision-assurance-as-a-byte-bound-independently-recomputed-chain.md), and [0096](decisions/0096-reconstruct-the-successor-phase-two-binding-before-technical-review.md) retain the immutable, unissued 0.1 resources plus five unissued successor schemas, seven schema-valid successor fixtures, 14 content-addressed synthetic backing preimages independently rederived from retained bytes, 44 expectation-free vectors, 49 downstream chain known-bads, and an exact frozen-source census. Three source-separated, non-sharing evaluators pass 132 exact recomputations: 44 each under Python 3.14.2, Node.js 24.18.0, and Temurin Java 21.0.9. Comparison covers all six projection fields: `participant_id`, `domain_results`, `categorical_results`, `categorical_failures`, `final_disposition`, and `reason_codes` | This is retained T0 byte-bound/recomputation candidate evidence, not final PRQ-013 closure. The eleven-round correlated, context-isolated, non-accountable model-worker report records `no_grounded_refutation_observed_within_declared_attacks` for exact scope `.0005`; round eleven reproduced closure observation `.0003`, install observation `.0004`, 25 context-review controls including the live generator path, two integration-truth controls, exact-bound regeneration, and full integration within its declared checks. The report is not a `ReviewDetermination` or organizationally independent review and does not establish correctness. All `HDA-CTX-001` through `HDA-CTX-016` findings remain pending accountable closure review. Fixed times are deterministic fixture times only; no real ceremony occurred. The 0.1 resources and all successors remain unissued. T1 `AuthorityAssignment`, T2 authority/currentness/quorum/consumer contracts, an unissued `AssuredDecision` wrapper, migration, end-to-end consumer refusal, accountable review, and operator acceptance remain absent. A real ceremony is separately authorized Gate B evidence, not a Gate A prerequisite |

The current 144-resource candidate schema set is included in the declared
199-member PRQ-002I universe and its current schema-reference edges resolve
offline under ADR 0109. That bounded result does not pretend to be a complete
historical resource archive or a product registry. The exact
`canonical-work-lease:0.1.0` resource is
now present as an unissued, identity-unresolved candidate owned by `work`; it
does not authorize a lease or execution. The canonical profile core/evidence
schemas are likewise unissued candidates owned by `canonical`; their external
raw-byte binding is review evidence, not canonical identity. The WorkIntent
core and external candidate-evidence schemas are unissued `work` resources;
they preserve WorkIntent 0.1 and its exact consumers while proving the
prospective nonrecursive construction.
The three 0.1 human-decision-assurance schema resource identities remain
immutable and unissued, and the five ADR 0095 successor identities are also
unissued. Their static framing profile, content-addressed synthetic preimages,
three non-sharing recomputation paths, exact six-field comparison, consumer
census, and adversarial corpora are retained architecture evidence; no current
decision consumer or human-only slot accepts them.
The side-by-side WorkIntent 0.2, canonical WorkLease 0.2, and WorkContract 0.3
successors retain those predecessors and bind exact dependency edges without
claiming canonical identity or admission. WorkIntent 0.3 now replaces the
three placeholder reference values with exact raw candidate/schema bindings;
its target and WorkIntent canonical digests, admission, and authority remain
absent. The current compatibility tranche separately reissues ResearchEvent
0.18.0 and blocked WorkContract 0.19.0, then reidentifies the profile-bound
WorkContract wrapper as 0.20.0. The wrapper binds the exact 0.19.0 bytes while
leaving construction, canonical digest, member, admission, acceptance, and
authority absent. These are new immutable resource identities, not aliases;
their predecessor bytes remain Git/reissue-ledger evidence rather than a
complete offline resolver. Four historical resource identities
still have no matching local current resource:
`command-contract-registry:0.1.0`, `command-receipt:0.3.0`,
`work-contract:0.1.0`, and the deliberately retained exact
`command-envelope:0.4.0` vocabulary source. Their bytes are not fabricated or
silently redirected to newer IDs. Exact current-universe `$ref` replay and the
three declared digest-binding shapes are closed within PRQ-002I's scope.
Resolution of the four historical identities, complete transitive consumer
migration, versioned retention beyond the declared universe, product identity,
profile issuance, accountable review, and operator acceptance remain Gate A
blockers.

The bounded scope, cohorts, C5 refusal boundary, and construction gaps are in the [First-Slice Admission Resolution Candidate](FIRST_SLICE_ADMISSION_RESOLUTION_2026-07-16.md). Its [machine inventory](../architecture/first-slice-admission-resolution-candidate.json) is exact architecture evidence, not an admitted registry. The earlier [closure plan](FIRST_SLICE_ADMISSION_CLOSURE_PLAN.md) is retained as the pre-resolution audit trail.

### Repository-governance activation observation

The repository-release row preserves the pre-hardening observation as history.
Fresh read-back on 2026-07-19 then established a distinct, bounded checkpoint:
base `56e8062334fb81bba955ba137be690e085d4c88e` remained public `main`, and
its sole child `a25d026bd7233dfc452accc6087ded0bf015d7b4` became the permanent
bootstrap release ref after all four workflows and ten jobs succeeded on
attempt 1. Active no-bypass rulesets are `immutable publication candidates`
(ID `19178198`) and `main exact-SHA fast-forward` (ID `19178503`).
Pull requests are disabled; merge and squash are false; rebase alone remains
true as the inert linear-history prerequisite; Actions require full-SHA pins
and read-only workflow tokens. The retained candidate-governance,
candidate-checks, and eleven-operation mutation-journal SHA-256 values are
`518dd562d0a9be561e9d732d66778f2f034914980625a891e612e83ee2f49060`,
`e60cadf4823ef4a7f07123d47f68434f247ec53dab5dc89bf58d9fe1fac93652`,
and `00b9661b0e4a316099c56432b99df6a3526382539ee01d15fe9ebe43e2d55827`.
Distinct final candidate `f1f25fd336daa1dd2707ba36b832e8d5c5e41d3e`
subsequently completed the first candidate-to-main activation: its permanent
release-ref and post-main workflow censuses each passed four workflows and ten
jobs, same-SHA promotion settled, remote-main replay compared equal, and the
read-only activation receipt has SHA-256
`2f9564a23bf3bde851244224eb5f69b5cfe881e39eec46b1387d5a34b85a1ab2`.
This is bounded repository-publication evidence for that exact subject only.
Gate A and runtime stay blocked.

## Readiness by Gate A area

| Gate | Current state | What still blocks acceptance |
| --- | --- | --- |
| G0 mission/boundary | Partial | The intended first user, one rights-cleared real-world wedge and decision, per-artifact rights, exact import root, final company/user language, accepted value-claim boundary, and operator sign-off |
| G1 scientific/mathematical | Partial | Registry enrollment, cross-record/unit/method resolution, independent implementations, broader property vectors, and statistical/physical-domain review |
| G2 domain/lifecycle | Blocked | The lease-release/claim contradiction is corrected in the unissued 0.18/0.19/0.20 reissue chain, and PRQ-002B now retains schemas for four standalone members, one commitment, and four registries, but it constructs zero identities or instances. C5/PRQ-009 still lacks a resolved assignable WorkIntent and complete assignment cohort; after closing it, construct dependency-closed payload, command, event, state, reducer, and registry members and run composite atomicity/replay evidence |
| G3 security/authority | Blocked | PRQ-013 now retains an unissued byte-bound/recomputation tranche: exact backing-artifact bytes, 44 answer-free vectors, three source-separated non-sharing implementations with 132 exact recomputations, complete six-field agreement, and 49 chain known-bads. The retained eleven-round report records `no_grounded_refutation_observed_within_declared_attacks` for exact scope `.0005`; the Gate and live-generator object readers, corrected PRQ-013 truth bindings, 25 context-review controls, exact-bound regeneration, and full integration survived only the declared round-eleven checks. It is correlated, non-accountable model-worker evidence, not correctness, a `ReviewDetermination`, or organizationally independent review. All `HDA-CTX-001` through `HDA-CTX-016` findings remain pending accountable closure review. The receipt/authenticator co-binding and all backing bytes are synthetic construction evidence; no real ceremony is measured. T1/T2 dependencies, unissued wrapper/consumer successors, transitive migration, end-to-end consumer refusal, accountable security/authority review, and the operator's exact-byte Gate A decision remain open. A real protected ceremony is a separately authorized Gate B probe; accepted root/assignment/grant/quorum members and real policy instances remain absent |
| G4 evidence/memory | Blocked | The frozen `odeya-jcs-0.1` predecessor and scoped `odeya-jcs-0.2` successor remain unissued. PRQ-002F–I now retain bounded two-implementation numeric traces/cross-object conformance, profile-bounded serialization, nine-domain governance, and dependency-closed offline resolution for the declared 199-member universe and three binding shapes. Four historical identities, product/member identity, profile issuance/admission, complete consumer migration, accountable profile review, the operator decision, witnessed retained roots, exact provenance/export mappings, and deletion/legal-hold/recovery replay remain absent |
| G5 evaluation/learning | Partial | The proposed frontier-value protocol now names comparator, budget, contamination, threshold, expiry, expert-outcome, and known-bad requirements, but it is not accepted or executable. Pinned rights-cleared tasks and holdouts, exact record contracts, validated graders, accountable review, and matched-budget or prospective outcome evidence remain absent |
| G6 modules/interfaces | Partial | The logical manifest passes, but real registry records, source/package import enforcement, port conformance, and handler equality do not exist |
| G7 reliability/operations | Partial | Deployment-profile isolation/locking, RPO/RTO, witness/key/trust profiles, restore/fork trials, and independent distributed-systems review |
| G8 UI/publication projections | Partial | Daniel’s exact design decision, independent reducers, fanout/noninterference and leakage tests, accessibility/human-factors review, and exact public release-settlement equality |
| G9 implementation order | Blocked | The architecture-scope counts are exact, but no Gate B probe or Gate C increment may start until immutable members, P0/root/activation, proof, review, Gate A decision, and explicit operator authorization exist |
| G10 red team/review | In progress | Close all critical/high findings on exact bytes; obtain accountable statistical, physical, security/privacy, distributed-systems, and accessibility determinations |

No row is passed.

## Critical blockers

| ID | Severity | Current blocker | Required closure evidence |
| --- | --- | --- | --- |
| A-001 | Critical | The exact 43/60/25 representational scope exists, but no immutable admitted member set or activation exists | Build and cross-check the exact payload → state → reducer → event → command member graph and admitted-only equality |
| A-002 | Critical | Exact construction gaps are 42 payloads, 43 command records, 60 event records, 25 state records, and 25 reducer records | Construct them in dependency order with exact versions, refusal vectors, independent replay, and member-set commitments |
| A-003 | Critical | C1/C4/C6 choices, structural grant slots, and one bounded composite authority/resource/race model now pass, but exact member records, executable refinement/replay, and independent evidence are absent | Typed observation/settlement members, exact slot occurrences, database/reducer traces for both race orders, refinement mapping, heterogeneous-unit checks, and independent review |
| A-004 | Critical | C2/C3 producer and atomic-cohort choices are resolved, but prospective exact event/payload members and atomic replay traces are absent | Exact correction-fence and validity-pair versions, no-interior-frontier enforcement, known-bads, two reducers, and replay quarantine evidence |
| A-005 | Critical | C7/C8 are bounded and the release/claim compatibility defect is corrected, but C5/PRQ-009 remains unresolved: WorkIntent 0.3 has exact raw targets but no admitted canonical identity or complete assignment binding; exact local execution/reducer members, materialization/refinement replay, and a real witnessed P0 activation instance are also absent | Reissued admitted WorkIntent/WorkContract/ResearchEvent/EventContractRecord identities; exact 13-event assignment, lease/materialization/sandbox/attempt records; sole Verification reducer record; clear root/C0/checkpoint/witness/P0 chain; composite recovery traces; and independent review |
| A-006 | Critical | Publication settlement is not independently proven replay-complete | Exact seal/grant/effect identities, both arrival orders, ambiguity/reconciliation, withdrawal/correction fanout, and projection equality |
| A-007 | High | Semantic rules are specified but not admitted with two implementations | Frozen registry entries, exact inputs/outputs, known-bad vectors, cross-object traces, and discrepancy handling |
| A-008 | Critical | The frozen predecessor audit remains exact and PRQ-002F–I now retain bounded dual-implementation trace, cross-object conformance, serialization, nine-domain, and declared-universe offline-resolution evidence over the unchanged `odeya-jcs-0.3` construction. The 0.2 and 0.3 profiles remain unissued; four historical identities remain unresolved; organizational independence and independent-host reproduction are absent. Existing root/C0/activation candidates can digest evidence references, but no retained proof establishes a complete, non-omittable admission mapping from each stable member-content digest to the exact conformance, trace, and review bundle used for admission | Preserve the 120/216 predecessor and every exact 0.2/0.3 byte; retain PRQ-002F–I evidence without widening its declared scope; specify and prove admission-evidence completeness and commitment; obtain accountable non-author review and the operator's exact profile decision; only then may separately authorized work issue exact profile/member identities, rerun conformance on the issued subject, and pursue admission |
| A-009 | High | The composite proof slice is not rights-settled or sealed and has not been replayed through Odeya | Per-artifact decisions, immutable import manifest/root, two clean verifier paths, composite replay/recovery/fanout package |
| A-010 | High | Threat and privacy assumptions lack accountable external review | Closed review of trust zones, prompt/data attacks, secrets, authority, witnesses, recovery, projections, and effects |
| A-011 | High | Mathematical and physical contracts lack accountable expert review and real-world evidence | Named statistical/metrology/VVUQ/safety review, accepted limitations, admitted methods, and real/synthetic falsifiers |
| A-012 | High | Key, witness, storage, retention, deletion, and recovery profiles remain deployment decisions | Accepted deployment-neutral contracts plus exact first-profile decisions and restore/fork evidence |
| A-013 | Medium | Product/technology candidates are not proven | Only explicitly authorized reversible Gate B probes with named evidence and exit rules |
| A-014 | Medium | Projection and UI semantics are not accepted or independently tested end to end | Daniel-approved surface contract; accessibility, leakage, cache, timing, stale-state, correction, and release fixtures |
| A-015 | Critical | No immutable architecture candidate has been independently signed | Clean exact candidate commit/manifest, closed critical/high findings, review determinations, and Daniel’s accept/reject/amend decision |
| A-016 | Critical | Current decision consumers remain unmigrated and can still accept their existing structural evidence without the new assurance wrapper. The corrected T0 successor retains exact synthetic backing-byte verification, three source-separated non-sharing eligibility implementations, and eleven-round correlated context-isolated technical-review evidence with no grounded refutation observed within its declared attacks. Organizational independence is not proven, and all 16 `HDA-CTX` findings remain pending accountable closure review. The signed phase-two challenge commits to the confirmation receipt only as a synthetic construction property; no real same-person ceremony was measured | Preserve exact root `.0005` equality and carry `HDA-CTX-001` through `HDA-CTX-016` into accountable security and authority review without relabelling the model-worker report as correctness, a `ReviewDetermination`, or independent review. Construct T1 `AuthorityAssignment` and the required T2 authority/currentness/quorum/consumer dependencies. Only then construct separately identified unissued `AssuredDecision` and successor-consumer candidates, prove exact transitive migration and end-to-end refusals, obtain accountable review, and submit the exact bytes for the Gate A issuance/admission decision. A real ceremony follows only as a separately authorized Gate B probe |

## Machine-family maturity

| Family | Current role | Maturity |
| --- | --- | --- |
| Command envelope/receipt 0.5.0/0.4.0 | Structural request/member/activation and settlement shapes with unresolved subject identities and comparison-probe digests | Nonconstructible, not admitted, and unusable for dispatch or settlement; a future resolved resource reissue is required |
| Command record/registry 0.1.0/0.2.0 | Standalone member and registry-candidate shapes with independent command, payload, and schema identity axes | Structural only; record/registry/canonical/closure/membership/activation identities remain unresolved |
| Work identity predecessor/successor cohort | Retains the WorkIntent and WorkLease predecessor/successor chain and now carries blocked WorkContract 0.19.0 plus its profile-bound wrapper 0.20.0 | Exact raw source/planning candidates and the output schema are bound, with raw lineage kept distinct from canonical identity. Profile issuance, target and WorkIntent canonical digests, admission, assignment, and the complete cohort remain blocked. Neither WorkContract resource is constructible or executable |
| PRQ-002B product-identity/profile successor | Four standalone member schemas, one ordered-map commitment, four pure registries, and a scoped `odeya-jcs-0.2` core/evidence/migration chain over an exact 120+12 cohort | Schema-valid exact-byte candidate only; structural vectors are nonidentity fixtures, zero product identities or instances exist, both profiles are unissued, PRQ-002 and Gate A remain blocked |
| PRQ-002C raw-number observation | Architecture-only raw-token contract, 61 opaque answer-free synthetic frames, two source- and language-separated staged results, two self-attested retained execution receipts, and one exact comparison receipt | Bounded integer-type/integer-const agreement only; 0.2 remains frozen, unissued, and blocked from conformance and issuance; the separate 0.3 construction does not extend this observation; full profile conformance, identity, admission, PRQ-002 closure, Gate A, and runtime remain absent |
| PRQ-002D prehash registry replay | Exact-predecessor contract, 68 opaque virtual-file frames, one private oracle, two source- and language-separated complete results, two self-attested execution receipts, one exact comparison receipt, and 77 single-attributed parent-gate known-bads | Bounded synthetic two-member prehash replay only; no process sandbox, independent witness, profile conformance, product identity, registry admission, PRQ-002 closure, Gate A acceptance, or runtime exists. The later PRQ-002E construction does not enlarge this replay's scope |
| PRQ-002E profile construction | Twelve final-only side-by-side schemas, nine structural-nonidentity fixtures, three ordered candidate records, and a separate seven-output observation transaction over the fifteen final schema-and-record subjects | Exact-byte construction agreement only; later F–I evidence does not amend these bytes or create identity, issuance, admission, organizational independence, accountable review, operator acceptance, PRQ-002 closure, Gate A, or runtime |
| PRQ-002F numeric-trace conformance | Complete raw-aware traces and cross-object conformance over the fifteen frozen construction subjects through two source-separated implementations and a third-path validator | Bounded candidate conformance evidence only; no product canonicalization, identity, issuance, admission, or wider subject claim |
| PRQ-002G serialization conformance | Profile-bounded serialization and refusal evidence through source-separated Python and Node implementations, exact comparison, and a third-path validator | Exact admitted-token/profile interpretation only; not general ECMAScript-number or product conformance |
| PRQ-002H product-domain governance | Nine frozen product-domain nonidentity frames each accepted by its governing schema with complete traces and per-domain refusal evidence through two implementations | One frozen nonidentity frame per domain only; no product member or registry instance exists |
| PRQ-002I offline resolution | Declared 199-member universe with 6,456 offline schema-reference edges and 241 declared digest bindings replayed twice and checked by a third path | Dependency-closed only for the declared universe and binding shapes; four historical identities, product identity, issuance, admission, review, and operator acceptance remain open |
| Research event/trace 0.18.0/0.3.0 | Closed event branches and replay/adverse traces; 0.18.0 preserves a claimed reservation across WorkLease release for later ResourceLedger settlement | Structural candidate with retained-fixture dereference only; per-branch payload contracts, real EventContractRecords, reducers, and complete replay remain absent |
| Registry/root/C0/activation 0.1.0 | Acyclic subject graph and prospective bounded activation | Structural candidate only |
| VerificationPackage 0.2.0 | Immutable terminal profile-relative verification result | Structural candidate; real assignment/replay/review absent |
| Cognitive family 0.1.0 | Non-authoritative state views, proposals, transitions, work/model/routing evidence | Local structural/semantic candidate |
| Projection family 0.1.0 | Rebuildable snapshot, redaction, equivalence, and impact records | Local structural/semantic candidate |
| Physical family 0.1.0 | Quantities, uncertainty, assets, measurements, models, evidence, safety, experiment proposal | Local structural/semantic candidate |
| Mathematical family 0.1.0 | Prospective estimand and proof-carrying scientific result envelope | Local structural/semantic candidate |
| Data/recovery/publication families 0.1.x | Rights/use/exposure/lifecycle, checkpoint/restore, release settlement | Structural and bounded-formal candidates; no live instances |

Schema validation proves only the declared structural language and bounded local predicates. It does not prove state legality, scientific truth, authority, independence, rights, external effects, or release visibility.

## Deliberately not started

- no engine runtime, application services, or production UI;
- no database, workflow, object-store, policy, sandbox, model, or laboratory provisioning;
- no engine/runtime provider, MCP, mission-repository, research-publication,
  payment, trading, or physical integration; evidence-gated architecture
  publication is the sole repository-effect exception;
- no DNS/domain/company action and no repository effect outside the
  exact-commit architecture-publication contract;
- no source import from Sentinel, Telos, Inbar, Maestro, or Aweb;
- no training, fine-tuning, recursive self-modification, deployment,
  scientific-results publication, or physical authority.

Architecture validators, schemas, fixtures, bounded models, and review plans are allowed because they test design bytes without performing research or external effects.

## Long-range path from checkpoint to candidate

This is the whole-program dependency map, not a license to skip the active
lane. The current-lane entry is item 1; the exact recovery gate, exit
conditions, and nonclaims live in `docs/SESSION_HANDOFF.md`.

1. Preserve the frozen 120/216 predecessor audit, every exact 0.2/0.3 byte,
   and the bounded PRQ-002F–I trace, serialization, domain-governance, and
   declared-universe resolution evidence. Specify, commit, and prove a complete,
   non-omittable mapping from every stable member-content digest to the exact
   conformance, trace, and review bundle used for admission; resolve the four
   historical identities or retain them explicitly outside an accepted scope;
   then obtain accountable non-author review before the operator's exact profile
   decision. The handoff defines this boundary precisely.
2. Treat the machine-checked 43/78 command, 60-event, 25-aggregate, 11-owner, one-P0 scope as the sole construction candidate; any scope change restarts this audit.
3. Build the 42 payload, 25 state, 25 reducer, 60 event, and 43 command records in dependency order with exact cohort/authority alternatives.
4. Instantiate reviewed registry/root/C0/checkpoint/witness/P0/activation candidates and prove exact member/handler equality.
5. Execute two non-sharing reducer paths and two separately isolated
   proof-mission verifier paths, retain their independence vectors, and test
   replay, recovery, correction fanout, and refusal cases.
6. Preserve ADR 0095's completed bounded adoption-evidence checklist at exact
   reviewed root
   `sha256:97062b38a14d5bdccf5ad87c547c62388e7cd82256a445f631856aecee54e1d9`.
   Keep the report's correlated, non-accountable, non-human boundary explicit,
   and carry `HDA-CTX-001` through `HDA-CTX-016` into accountable closure review; do not reinterpret
   132 passing synthetic recomputations as ceremony or authority.
7. Construct T1 `AuthorityAssignment` and the required T2
   authority/currentness/quorum/consumer contracts. Only then construct
   unissued wrapper/successor candidates, prove exact migration and end-to-end
   refusal, and obtain accountable security/authority review.
8. Produce an exact candidate manifest and clean commit; rerun every suite against those bytes.
9. Ask Daniel to accept, reject, or amend the Gate A candidate. Acceptance
   alone does not authorize a live ceremony or runtime product work; those
   remain separately authorized Gate B and Gate C stages.

The architecture is materially stronger and far more falsifiable than the starting foundation. The honest status remains: **working checkpoint, Gate A blocked**.
